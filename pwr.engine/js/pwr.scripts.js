var pwrScripts = {		loadInto : function(url,targetId){		$("#"+targetId).html("<div id=\"loading_box\">Caricamento...</div>");		$("#"+targetId).load(url);	},		submitInto : function(formId,url,targetId){		var data = $('#'+formId).serialize();		$.ajax({			type: "POST",			url: url,			data: data,			success: function(response){				$("#"+targetId).html(response); 			}		});		//$.post(url,data)		//	.success(function(result){		//		targetElement.html(result);		//	});		return false;	},		checkFormField : function(id,type,isNeeded){		var isValidField = true;		var element = $(id);				switch(type){			default:				break;		}				if(isNeeded && element.val()=="") isValidField=false;				if(isValidField){			element.removeClass("invalid");		}else{			element.addClass("invalid");		}				return isValidField;			},				getUrlVar: function($key){		var vars = {};		var parts = window.location.href.replace(/[?&]+([^=&]+)=([^&]*)/gi, function(m,key,value) {			vars[key] = value;		});		return vars[$key];	}	};